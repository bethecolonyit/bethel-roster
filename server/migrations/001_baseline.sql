-- 001_baseline.sql
-- Baseline stamp migration.
-- The schema already exists (migrated manually from SQLite).
-- Do not add schema changes here. Add new migrations after this point.

SELECT 1 AS baseline;
